#!/bin/bash
# insta485db

# stop on errors
set -Eeuo pipefail

# call insta485db create
./bin/insta485db create

# set her up
export FLASK_ENV=development
export FLASK_APP=insta485
flask run --host 0.0.0.0 --port 8000